# 文档摘要生成器

一个命令行小工具，读取 txt 文件，输出文件统计信息和前200个字符（模拟摘要）。  
代码框架已经写好：文件读取、异常处理、日志、命令行参数都有了。  
因为大模型 API 额度还没搞定，摘要部分暂时是模拟的，但结构可以随时换成真实 API。

## 目前能做什么
- 读取指定 txt 文件
- 统计字符数、单词数、行数
- 输出前200个字符作为“模拟摘要”
- 如果文件不存在会报错，不会崩溃
- 有日志记录

## 怎么用
```bash
python summary_generator.py sample.txt
